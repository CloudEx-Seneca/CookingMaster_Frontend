import React, { useState } from 'react';
// import React from 'react';
// Needs to show profile page with User Name and Written/Saved/Liked recipes

const Profile: React.FC = () => {
    return(
      <div>
        <h2>Profile Page</h2>
        <p>User's info general info and recipes liked, saved and created (Testing phase).</p>
      </div>
    );
};

export default Profile;